import React from 'react';
import ReactDOM from 'react-dom/client';
import './emilito/index.css';
import App from './emilito/App.jsx';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);